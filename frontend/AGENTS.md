# Scope

This task applies ONLY to the frontend directory.

Never modify files outside the frontend folder.

Do not read, edit, rename, or delete any files in the backend folder.

The backend APIs are considered stable and must not be changed.

Only modify frontend code, styles, assets, and reusable UI components.
